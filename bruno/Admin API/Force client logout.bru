meta {
  name: Force client logout
  type: http
  seq: 8
}

post {
  url: {{serverUrl}}/api/v1/admin/users/:userId/logout/:clientId
  body: none
  auth: inherit
}

params:path {
  userId:
  clientId:
}
