meta {
  name: 1 - Sign In
  type: http
  seq: 2
}

post {
  url: {{serverUrl}}/api/v1/flow/sign-in
  body: json
  auth: apikey
}

auth:apikey {
  key: Authorization
  value: State {{state}}
  placement: header
}

body:json {
  {
    "login": "{{login}}",
    "password": "{{password}}"
  }
}
