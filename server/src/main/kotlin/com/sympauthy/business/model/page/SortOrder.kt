package com.sympauthy.business.model.page

/**
 * The direction the key a listing sorts on is read in.
 */
enum class SortOrder {
    /** From the smallest value to the largest, which is the direction a listing takes by default. */
    ASC,

    /** From the largest value to the smallest. */
    DESC
}
