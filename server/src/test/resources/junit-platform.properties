# Fail a test that leaves a stub unused. See docs/testing-standard.md.
mockk.junit.extension.checkUnnecessaryStub=true

# Name a parameterized test after the database it ran against. The method is already the name of the
# container above it, so a test that spelled its own name here would spell it twice.
junit.jupiter.params.displayname.default={0}
